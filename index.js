const service = require('./service/html');
const sql = require('./service/sql');
const db = require('./util/mysql');

// service.getBingBgUri(1).then(res => {
//   res.forEach(item => {
//     sql.insert('wallpaper', item, db.wallpaperQuery);
//   });
// });
